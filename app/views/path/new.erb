<div class="inner cover">
    <h1 class="cover-heading">Plan a New Path</h1>
    <p class="lead">A path is a vision of your Stanford career, whether you're just picking a major or planning out two concentrations, a minor, and a coterm.
    <br>You can explore many paths.
    <p class="lead">
        <form class="form-signin form-signup" action="/paths" method="POST">
            <%= csrf_tag %>
            <input class="form-control" type="text" name="name" placeholder="Name this path" required maxlength="20">
            <% @tracks.each do |track| %>
                <div class="checkbox">
                    <label>
                        <input type="checkbox" name="tracks[<%= track.id %>]" value="1"> <%= pp track %>
                    </label>
                </div>
            <% end %>
            <button type="submit" class="btn btn-default">Let's get started</button>
        </form>
    </p>
    <p class="lead">
    </p>
</div>
